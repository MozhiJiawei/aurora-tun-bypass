interface:
  display_name: "Aurora TUN 直连维护器"
  short_description: "安全检查并维护 Aurora TUN 的进程直连规则"
  default_prompt: "Use $aurora-tun-bypass to add selected Windows processes to Aurora TUN direct-routing rules with backup and runtime validation."
